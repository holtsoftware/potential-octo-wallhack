#include <iostream.h>
#include <ap/apvector.h>
#include <math.h>


class binary{
public:
	binary();
	void operator=(int);
	void output();
	void output(ostream &);
	int decmal();
private:
	apvector<bool> bin;
};